function App() {
  const name = "Jetty";
  const style = {
    backgroundColor: "black",
    color: "aqua",
    fontSzie: "48px",
    fontWeight: "bold",
    padding: 16,
  };
  return <div style={style}>{name}</div>;
}

export default App;
